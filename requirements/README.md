# Requirements folder

Place the project's markdown requirements file here.

Recommended filenames:
- `requirements.md`
- `app-requirements.md`
- `study-task-planner-requirements.md`

The `requirements-to-github-pages` skill expects an existing markdown requirements file and should not invent requirements from scratch.
