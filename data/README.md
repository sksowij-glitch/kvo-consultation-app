# Data folder

Use this folder for seed or demo data bundled with the application.

Typical pattern:
1. keep `seed.json` in this folder;
2. initialize browser persistence from it on first run;
3. provide a visible `Reset demo data` control.

Do not treat repository files as the main runtime persistence store.
